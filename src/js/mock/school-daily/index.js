let mockList=[];
mockList=mockList.concat(require('./course-plan'));
mockList=mockList.concat(require('./my-daily'));
mockList=mockList.concat(require('./professional-rules'));

module.exports = mockList;
